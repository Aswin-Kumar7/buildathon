# Model card — card-testing risk model

Generated by `make eval`. Not hand-edited.

## What this is

The one deployed model: a calibrated binary logistic that scores an entity's risk of being card
testing / abuse, **P(abuse)**. It is linear and temperature-scaled, so the request path evaluates it
directly as a few dot products, and its per-feature contributions are exact. The number reported here
is this model's, on a held-out split of the same corpus it was trained on — there is no separate
benchmark model.

- Registry version `r1`, feature definition `fdv-bfe27ae905dd`,
  training data `sha256:b5f100ca3407d0cf`.
- 5,225 rows across 640 scenario groups; positive (abuse) rate 0.197.

## Held-out results

At the cost-optimal block threshold **0.080** (chosen to minimise expected cost from
false-negative ₹5,000 vs false-positive ₹800):

| Metric | Value (95% CI) |
|---|---|
| PR-AUC | 0.940 (0.919–0.957) |
| Precision | 0.721 (0.672–0.767) |
| Recall | 0.971 (0.952–0.989) |
| F1 | 0.828 (0.793–0.859) |
| ROC-AUC | 0.981 |
| Brier | 0.044 |

No-skill PR-AUC floor (prevalence): 0.211. Operating point:
false-decline rate 0.100, block-eligible 0.285, review band
0.029 of traffic at a review budget of 0.030.

## Where it is wrong

The per-origin breakdown, hardest first — the honest picture an aggregate hides. The model catches
the obvious attacks and struggles where card testing and a biller's dunning genuinely overlap.

| Origin | Kind | n | Score |
|---|---|---|---|
| aggressive_dunning | benign | 31 | FP-rate 1.000 |
| retry_storm | benign | 33 | FP-rate 0.909 |
| masked_in_outage | attack | 25 | recall 0.760 |
| flash_sale | benign | 54 | FP-rate 0.148 |

## Ablation ladder (PR-AUC)

| Features | Count | PR-AUC |
|---|---|---|
| all features | 10 | 0.940 |
| entity only (no traffic context) | 7 | 0.923 |
| traffic context only | 3 | 0.304 |

Removing the traffic-context features is what collapses the ability to tell an outage's failures from
a masked attack — the ladder shows it rather than asserting it.

## Leakage delta

Grouped-split PR-AUC 0.940 vs careless row-wise 0.907
(delta -0.033). On a corpus from a single seeded generator the gap is small — every
instance generalises to every other — which is the honest thing to report. The dramatic leakage story
belongs to the real-data IEEE-CIS research benchmark, not here.

## What this does not claim

- **These labels are synthetic, not real-world outcomes.** Synthetic scenario corpus, not real-world labels. Every row is an entity from a seeded scenario the project authored; the label is the scenario's ground truth, not a confirmed chargeback. Scores measure the deployed model on a held-out grouped split of this corpus. The path to real labels is the merchant's own confirmed incidents — see the retraining design.
- The model is load-bearing but leashed: it feeds the decision alongside the deterministic rules and
  arbitration, and can route a case to a person, but it never contains a shopper on its own.
