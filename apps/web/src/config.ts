/**
 * Demo credentials are shown in the UI on purpose.
 *
 * A reviewer clones the repository and must be able to sign in without anyone
 * provisioning an account for them. These accounts are seeded only when the user table
 * is empty and exist solely in development — there is nothing secret about them.
 */
export const DEMO_HINT = 'Demo: analyst@sentinel.local / sentinel-demo';
